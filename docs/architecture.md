# Architecture

SourceDraft uses this model:

Editor UI
→ Universal article schema
→ Validation
→ Publishing adapter
→ Publisher

The first adapter targets Astro MDX. The first publisher targets GitHub repository commits.
