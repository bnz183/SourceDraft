# Demo mode — copy to .env for local exploration without publisher credentials.
#   cp .env.demo.example .env
# Or run without editing files: pnpm demo
#
# Never commit .env.

SOURCEDRAFT_ADMIN_PASSWORD=admin
SOURCEDRAFT_DEMO_MODE=true

# Optional — defaults are fine for demo
STUDIO_API_PORT=8787
